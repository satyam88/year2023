apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dev-year2023-ingress
  namespace: dev
  labels:
    env: dev
    app: year2023-ms
    layer: frontend
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: year2023.connectingclouds.in
      http:
        paths:
          - pathType: Prefix
            path: "/"
            backend:
              service:
                name: dev-year2023-service
                port:
                  number: 8080